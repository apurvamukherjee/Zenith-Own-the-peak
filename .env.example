# Copy to .env.local for local dev, and add the same var in Vercel.
# Leave blank to run Zenith fully local (cloud sync simply stays disabled).
# `npx convex dev` writes this automatically for local development.
VITE_CONVEX_URL=
